# Example env values for local development
# Copy to .env and adjust if needed

# Postgres core used by some services and the BFF
POSTGRES_CORE_DB=alpespartners_core
POSTGRES_CORE_USER=admin
POSTGRES_CORE_PASSWORD=admin10100101.
POSTGRES_CORE_HOST=postgres-core
POSTGRES_CORE_PORT=5432

# BFF DB URL (used by entrypoint to run migrations)
DB_URL=postgresql+psycopg2://admin:admin10100101.@postgres-core:5432/alpespartners_core

# BFF settings
BFF_HOST=0.0.0.0
BFF_PORT=8000

# Start consumers via ENTRYPOINT
START_CONSUMERS=1
